# ============================================================================
# SEMAI CREW - ENVIRONMENT CONFIGURATION
# ============================================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env:    cp .env.example .env
# 2. Replace the placeholder values with your actual API keys
# 3. Never commit your .env file to version control!
#
# ============================================================================

# ----------------------------------------------------------------------------
# OPENAI API KEY (Required)
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# Used for: GPT-3.5-Turbo, GPT-4, GPT-4-Turbo agents
#
OPENAI_API_KEY=sk-your-openai-api-key-here

# ----------------------------------------------------------------------------
# MODEL SELECTION (Optional)
# ----------------------------------------------------------------------------
# Default model for agents. Can be overridden per-agent in agents.yaml
# Examples: gpt-4-turbo, gpt-3.5-turbo, gpt-4o
#
MODEL=gpt-4-turbo

# ----------------------------------------------------------------------------
# TOGETHER AI API KEY (Required for DeepSeek models)
# ----------------------------------------------------------------------------
# Get your API key from: https://api.together.xyz/settings/api-keys
# Used for: DeepSeek V3.1 agents (documentation_compliance_checker, etc.)
#
TOGETHER_API_KEY=your-together-api-key-here

# ----------------------------------------------------------------------------
# SERPER API KEY (Optional - for web search)
# ----------------------------------------------------------------------------
# Get your API key from: https://serper.dev/api-key
# Used for: Web search capabilities in agents
#
SERPER_API_KEY=your-serper-api-key-here

# ----------------------------------------------------------------------------
# CREWAI TRACING (Optional)
# ----------------------------------------------------------------------------
# Enable/disable CrewAI execution tracing for debugging
# Set to 'true' to enable detailed execution logs
#
CREWAI_TRACING_ENABLED=true

# ============================================================================
# NOTES:
# - OPENAI_API_KEY is required for most agents
# - TOGETHER_API_KEY is required if using DeepSeek models
# - SERPER_API_KEY is optional (only needed for web search tools)
# ============================================================================
